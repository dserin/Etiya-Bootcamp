import { CorporateFilterPipe } from './corporate-filter.pipe';

describe('CorporateFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new CorporateFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
