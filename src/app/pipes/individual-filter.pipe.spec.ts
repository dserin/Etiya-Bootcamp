import { IndividualFilterPipe } from './individual-filter.pipe';

describe('IndividualFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new IndividualFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
